#!/bin/bash

docker pull lscr.io/linuxserver/mariadb
docker run -d \
 --restart=always \
 --name mariadb \
 -h mariadb \
 -e PUID=1001 \
 -e PGID=1001 \
 -e MYSQL_ROOT_PASSWORD=$dbpass \
 --net=network \
 -p 3306:3306 \
 -v mariadb:/config \
 lscr.io/linuxserver/mariadb
